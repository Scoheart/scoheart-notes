#

## 如何代理 CLI 程序

``` bash
> npm install vue

$ npm install vue

>> npm install vue

~ npm install vue

 ~/ npm install vue
```

install 是把 npm 包拉回来
1. 从哪里拉？ registry
2. 拉回来之后，放到哪里？ 先放缓存目录，后放node_modules
